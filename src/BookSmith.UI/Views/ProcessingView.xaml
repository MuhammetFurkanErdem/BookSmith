<UserControl x:Class="BookSmith.UI.Views.ProcessingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Margin="24">
        <Border Style="{StaticResource CardStyle}" Width="480" Padding="32">
            <StackPanel HorizontalAlignment="Center">
                <!-- Status Icon / Title -->
                <TextBlock Text="⚡" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                <TextBlock Text="Cleaning Audiobook PDF..." FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,6"/>
                <TextBlock Text="{Binding FileName}" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,24" TextTrimming="CharacterEllipsis"/>

                <!-- Progress Bar -->
                <ProgressBar Value="{Binding ProgressValue}" Maximum="100" Height="8" Margin="0,0,0,16" Foreground="{StaticResource PrimaryBrush}" Background="#1A1A22"/>

                <!-- Dynamic Status Message -->
                <TextBlock Text="{Binding StatusText}" FontSize="13" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
