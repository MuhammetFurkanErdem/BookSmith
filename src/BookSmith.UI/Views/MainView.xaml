<UserControl x:Class="BookSmith.UI.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:BookSmith.UI.ViewModels.Main"
             xmlns:views="clr-namespace:BookSmith.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900"
             Background="{StaticResource BgDarkBrush}">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:ImportViewModel}">
            <views:ImportView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ProcessingViewModel}">
            <views:ProcessingView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:EditorViewModel}">
            <views:EditorView />
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top Header & Stepper Bar -->
        <Border Grid.Row="0" Background="#121215" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="24,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- App Title -->
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="BookSmith" Style="{StaticResource HeaderTextStyle}" FontSize="20"/>
                    <TextBlock Text="AI Book Cleaner for Audiobooks (v2.0)" Style="{StaticResource SubHeaderTextStyle}" FontSize="11"/>
                </StackPanel>

                <!-- Stepper Indicators -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <!-- Step 1 Indicator -->
                    <Border CornerRadius="12" Padding="12,5" Margin="0,0,8,0" Background="#1E1E26">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="1" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,6,0"/>
                            <TextBlock Text="Select PDF" FontSize="12" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- Step 2 Indicator -->
                    <Border CornerRadius="12" Padding="12,5" Margin="0,0,8,0" Background="#1E1E26">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="2" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,6,0"/>
                            <TextBlock Text="Cleaning" FontSize="12" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- Step 3 Indicator -->
                    <Border CornerRadius="12" Padding="12,5" Background="#1E1E26">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="3" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}" Margin="0,0,6,0"/>
                            <TextBlock Text="Editor &amp; Export" FontSize="12" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Dynamic Content Body -->
        <ContentControl Grid.Row="1" Content="{Binding NavigationService.CurrentViewModel}" />
    </Grid>
</UserControl>
